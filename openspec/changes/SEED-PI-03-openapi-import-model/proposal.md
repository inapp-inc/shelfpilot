# SEED-PI-03 — OpenAPI & import model contract

**Goal:** Document and validate extended `floorPlanImport` on POST `/layouts` per spec §8.

**Depends on:** SEED-PI-01 (field shapes)  
**Can parallel:** SEED-PI-04 once types are stable.

# Proposal: SEED-02-admin-config

## Why

Deliver the SEED unit **SEED-02-admin-config** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-02-admin-config.md`.

## What changes

Full Admin & Config (M6) wired to SQLite: users tab, store master, approval, configuration, audit.

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-02-admin-config.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-02-admin-config/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

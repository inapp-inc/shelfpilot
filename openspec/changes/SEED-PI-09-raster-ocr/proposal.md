# SEED-PI-09 — Raster: OCR & scale bar (PNG)

**Goal:** Server `POST /layouts/analyze-plan` accepts image upload; OCR text for Layout1/Layout2 PNG when PDF text unavailable; env-gated (NFR-3).

**Depends on:** SEED-PI-06  
**Out of scope:** External paid OCR unless configured via env.

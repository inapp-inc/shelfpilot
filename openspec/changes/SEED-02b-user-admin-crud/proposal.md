# Proposal: SEED-02b-user-admin-crud

## Why

Deliver the SEED unit **SEED-02b-user-admin-crud** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-02b-user-admin-crud.md`.

## What changes

Admin can create/update demo users in SQLite (mock passwords).

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-02b-user-admin-crud.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-02b-user-admin-crud/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

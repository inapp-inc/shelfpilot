# Proposal: SEED-08b-version-compare

## Why

Deliver the SEED unit **SEED-08b-version-compare** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-08b-version-compare.md`.

## What changes

Compare two layouts (or versions) for utilization and fixture count deltas.

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-08b-version-compare.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-08b-version-compare/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

# SEED-PI-07 — UI: create modal fixture preview

**Goal:** After upload, show parsed scale + runs table; let user confirm envelope / switch to envelope-only + packer fallback (FR-UX-01, FR-UX-02).

**Depends on:** SEED-PI-06, SEED-PI-05 (for end-to-end create)

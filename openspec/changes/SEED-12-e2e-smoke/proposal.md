# Proposal: SEED-12-e2e-smoke

## Why

Deliver the SEED unit **SEED-12-e2e-smoke** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-12-e2e-smoke.md`.

## What changes

Automated happy-path smoke: login → create → fixture → aisle → map → analytics.

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-12-e2e-smoke.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-12-e2e-smoke/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

# SEED-PI-04 — Service: runs → shelves & aisles

**Goal:** `buildLayoutFromFixtureImport(layout, floorPlanImport, options)` creates ShelfPilot shelves/aisles from parsed runs.

**Depends on:** SEED-PI-02, SEED-PI-03  
**v1 placement:** Grid/strip layout inside envelope when `geometry` absent; use fixture catalog defaults for depth/height.

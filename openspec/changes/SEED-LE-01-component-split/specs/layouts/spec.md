# Delta SEED-LE-01-component-split

### Requirement: SEED-LE-01-component-split

Extract Layout Editor into reusable components under web/src/layout-editor/ with behavior parity.

#### Scenario: AC-1
- **THEN** Given Designer opens editor, When page loads, Then modular components render and place/map still works.


# Proposal: SEED-07b-viz-2d-fidelity

## Why

Deliver the SEED unit **SEED-07b-viz-2d-fidelity** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-07b-viz-2d-fidelity.md`.

## What changes

2D editor visual parity with ui/ShelfPilot.dc.html.

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-07b-viz-2d-fidelity.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-07b-viz-2d-fidelity/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

# Proposal: SEED-04-layout-canvas

## Why

Deliver the SEED unit **SEED-04-layout-canvas** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-04-layout-canvas.md`.

## What changes

M1 layout canvas: scaled floor, zoom, selection, aisle tools with min-width validation.

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-04-layout-canvas.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-04-layout-canvas/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

# Proposal: SEED-01-auth-rbac

## Why

Deliver the SEED unit **SEED-01-auth-rbac** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-01-auth-rbac.md`.

## What changes

Mock email/password login with role selection and bearer-token RBAC.

## Status

Done

## Out of scope

See Out of scope in `Docs/seeds/SEED-01-auth-rbac.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-01-auth-rbac/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

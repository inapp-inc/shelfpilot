# Proposal: SEED-00-bootstrap

## Why

Deliver the SEED unit **SEED-00-bootstrap** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-00-bootstrap.md`.

## What changes

Scaffold ShelfPilot codebase with health API and project layout.

## Status

Done

## Out of scope

See Out of scope in `Docs/seeds/SEED-00-bootstrap.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-00-bootstrap/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

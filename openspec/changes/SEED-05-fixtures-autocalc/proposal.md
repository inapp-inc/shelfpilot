# Proposal: SEED-05-fixtures-autocalc

## Why

Deliver the SEED unit **SEED-05-fixtures-autocalc** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-05-fixtures-autocalc.md`.

## What changes

M2 fixture palette from vertical templates, place/edit/delete, auto-calc on dimension change.

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-05-fixtures-autocalc.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-05-fixtures-autocalc/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

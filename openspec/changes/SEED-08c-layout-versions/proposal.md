# Proposal: SEED-08c-layout-versions

## Why

Deliver the SEED unit **SEED-08c-layout-versions** as defined in `Docs/SEED_PLAN_FULL.md` and `Docs/seeds/SEED-08c-layout-versions.md`.

## What changes

Demo layout versioning: snapshot on submit-for-review; list versions for compare.

## Status

Todo

## Out of scope

See Out of scope in `Docs/seeds/SEED-08c-layout-versions.md`.

## Impact

- Demo stack only (SQLite · mock auth · Docker Compose)
- Spec delta under `openspec/changes/SEED-08c-layout-versions/specs/`
- Implementation under `codebase/` when this SEED is selected for build

## Parent change

`openspec/changes/shelfpilot-mvp/`

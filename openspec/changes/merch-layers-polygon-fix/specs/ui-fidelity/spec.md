## ADDED Requirements

### Requirement: 2D canvas mouse-wheel zoom
The 2D layout canvas SHALL support zoom via mouse wheel, preferably scaling toward the pointer, without scrolling the browser page while the pointer is over the canvas.

#### Scenario: Wheel zoom
- **WHEN** the user scrolls the mouse wheel over the 2D floor plan
- **THEN** the canvas zoom level changes and the page does not scroll

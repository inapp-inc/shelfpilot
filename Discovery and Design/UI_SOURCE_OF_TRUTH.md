# Visual SoT update

Canonical UI prototype is now `ui/ShelfPilot.dc.html`.

When generating or revising UI with Claude, open that file as the primary reference and keep brand tokens from `ui/brand.md`. Prefer refining the React port in `codebase/web` over inventing a new visual system.

See also: `Discovery and Design/Claude_UI_Generation_Brief.md` (prompt archive).
